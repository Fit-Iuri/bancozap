name: Clone Private Repository Workflow

on:
  push:
    branches:
      - "main"
    paths-ignore:
      - ".github/**"

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
        with:
          repository: Fit-Iuri/github-actions-training
          ref: refs/heads/whitelabel/BancoZap
          token: ${{ secrets.API_TOKEN_GITHUB }}
          fetch-depth: 0